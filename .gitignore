# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
venv/
.venv/
env/

# IDE / Editor
.idea/
.vscode/
*.swp
*.swo
*~

# Environment / Secrets
.env
!.env.example

# Docker
pgdata/

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Logs
*.log
pg_log/

# Test artifacts
.pytest_cache/
.coverage
htmlcov/

# Claude Code
.claude/

# Project planning docs (keep local)
多Agent架构调研与初始建议.md
工具和基建.md
项目概述.md

# Test workspace (temp/test/ — e2e test artifacts, not tracked)
temp/

# Local MCP configuration
.mcp.json

# Test runner scripts (development only)
oculai-mcp/run_test_*.py
oculai-mcp/run_pipeline.py
oculai-mcp/mcp_call.py
oculai-mcp/generate_report_*.py

# Pi runtime — pi.exe tracked via Git LFS; companion files committed normally.
# Only the runtime's own node_modules/ stays ignored (regenerable, not needed at runtime).
pi-windows-x64/node_modules/

# Desktop app — dependencies and build artifacts (not committed)
oculai-desktop/node_modules/
oculai-desktop/dist-electron/
